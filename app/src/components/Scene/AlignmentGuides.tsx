import React from 'react';

// Simple stub component to prevent import errors
// The complex alignment guide system has been temporarily disabled
export const AlignmentGuides: React.FC = () => {
  return null;
};

export default AlignmentGuides;